//nested if
#include<stdio.h>
int main()
{
    int a,b;
    printf("enter the number\n");
    scanf("%d%d",&a,&b);
    if(a>0)
    {
        if(b>0)
        printf("both are positive");
    }
    return 0;
}